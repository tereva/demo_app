<h1>Listing micropostes</h1>

<table>
  <tr>
    <th>Content</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @micropostes.each do |microposte| %>
  <tr>
    <td><%= microposte.content %></td>
    <td><%= microposte.user_id %></td>
    <td><%= link_to 'Show', microposte %></td>
    <td><%= link_to 'Edit', edit_microposte_path(microposte) %></td>
    <td><%= link_to 'Destroy', microposte, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Microposte', new_microposte_path %>
